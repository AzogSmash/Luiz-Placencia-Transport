// Ce fichier est conservé pour compatibilité mais n'est plus utilisé directement.
// Préférez supabase-browser.ts (client), supabase-server.ts (SSR) ou supabase-admin.ts (service role).
export { createAdminClient as supabase } from './supabase-admin'
